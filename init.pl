:- use_module(library(lists)). % list functionality, such as member/2
:- use_module(library(clpz)). % integer constraint programming
:- use_module(library(reif)). % has a useful filter predicate called tfilter
:- use_module(library(pairs)). % operations on key-value lists
:- use_module(library(dcgs)). % DCG functionality